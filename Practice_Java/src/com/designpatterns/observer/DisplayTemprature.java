package com.designpatterns.observer;/*
 * @author -Suraj Tiwari
 */

import javax.security.auth.Subject;

public interface DisplayTemprature {
     void update(float temp,float humidity,float presure);
}
