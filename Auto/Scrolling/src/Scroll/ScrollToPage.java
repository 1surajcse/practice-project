package Scroll;

public class ScrollToPage {
/*
	public static void main(String[] args) throws InterruptedException {
		System.setProperty("webdriver.chrome.driver", "D:/Auto/chromedriver.exe");
		WebDriver driver=new ChromeDriver();
	


	    Thread.sleep(100);
		System.out.println("loading the url");
		//driver.findElement(By.xpath("#account")).click();

		driver.get("http://manos.malihu.gr/repository/custom-scrollbar/demo/examples/complete_examples.html");
		driver.manage().window().maximize();
		Thread.sleep(5000);
		
		
		JavascriptExecutor je=(JavascriptExecutor)driver;
		 //scrolling  down-Whole page scrolling
		
		
		//scrolling inside the window
		WebElement ele=driver.findElement(By.xpath("//*[@id='mCSB_3_container']/p[7]"));
	 
	
		Thread.sleep(2000);
		
		System.out.println("scrolling inside the window");
		
		je.executeScript("arguments[0].scrollIntoView(true);", ele);
		
		
		
		//Using Action class:
	    Actions dragger = new Actions(driver);
	    
        WebElement draggablePartOfScrollbar = driver.findElement(By.xpath("//*[@id='mCSB_3_dragger_vertical']"));

        // drag downwards
        int numberOfPixelsToDragTheScrollbarDown = 50;
        for (int i=10;i<500;i=i+numberOfPixelsToDragTheScrollbarDown){
            try{
        // this causes a gradual drag of the scroll bar, 10 units at a time
        dragger.moveToElement(draggablePartOfScrollbar).clickAndHold().moveByOffset(0,numberOfPixelsToDragTheScrollbarDown).release().perform();
        Thread.sleep(1000L);
            }catch(Exception e1){}
        }
		
        
        // now drag opposite way (downwards)
        numberOfPixelsToDragTheScrollbarDown = -50;
        for (int i=500;i>10;i=i+numberOfPixelsToDragTheScrollbarDown){
        // this causes a gradual drag of the scroll bar, -10 units at a time
        dragger.moveToElement(draggablePartOfScrollbar).clickAndHold().moveByOffset(0,numberOfPixelsToDragTheScrollbarDown).release().perform();
        Thread.sleep(1000L);
        }
		
		//System.out.println("verify::"+ele.getText());
		

	}*/

}
